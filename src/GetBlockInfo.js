// import { Alchemy, Network } from 'alchemy-sdk'
// import React, { useEffect, useState } from 'react'
// import reactRouterDom from 'react-router-dom'


export default async function GetBlockInfo(props) {
    // const [response, setResponse] = useState([])
    //  useEffect(() => {
    //     setResponse(prevResponse => prevResponse = props.alchemy.core.getBlock(props.blockNumber))
    // }, [props.blockNumber])
//     let response = await props.alchemy.core.getBlock(props.blockNumber)

//     console.log(response)
}
